{
  "name": "blackbox-investigation-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install --include=dev && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  }
}
