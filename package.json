{
  "name": "blackbox-investigation-platform",
  "version": "1.0.0",
  "description": "Digital Evidence Investigation Platform",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install --include=dev && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongodb-memory-server": "^11.2.0",
    "mongoose": "^9.9.4"
  }
}